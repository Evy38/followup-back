nelmio_api_doc:
  documentation:
    info:
      title: 'FollowUp API'
      description: 'Documentation interactive de l’API FollowUp (Symfony + JWT)'
      version: '1.0.0'
    components:
      securitySchemes:
        Bearer:
          type: http
          scheme: bearer
          bearerFormat: JWT

    security:
      - Bearer: []

  areas:
    path_patterns:
      - ^/api(?!/doc$)
