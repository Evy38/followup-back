# --- Variables Docker / Base de données ---
# Mot de passe root de MySQL (utilisé pour créer la DB au démarrage)
MYSQL_ROOT_PASSWORD=root
# Nom de la base créée par défaut
MYSQL_DATABASE=followup_db
# Utilisateur créé pour l'application
MYSQL_USER=follow_user
# Mot de passe de l'utilisateur application (éviter d'utiliser "root" en prod)
MYSQL_PASSWORD=root
# Fuseau horaire pour le container MySQL
TZ=Europe/Paris

# --- Variables Symfony / application ---
# ENV Symfony (dev ou prod)
APP_ENV=dev
# Clé secrète symfony (génère-en une pour prod)
APP_SECRET=CHANGE_ME

# DATABASE_URL utilisée par Symfony pour se connecter à MySQL
# Format : mysql://<user>:<password>@<host>:<port>/<dbname>
# Avec Docker Compose, le host sera `db` (nom du service) quand Symfony tourne dans le container php
DATABASE_URL=mysql://follow_user:root@db:3306/followup_db

# Pour phpMyAdmin : accès depuis l'hôte sur http://localhost:8081 (PMA)
# PMA utilise PMA_HOST, PMA_USER, PMA_PASSWORD (déjà dans docker-compose.yml)
# Tu peux surcharger ici si nécessaire :
PMA_HOST=db
PMA_USER=root
PMA_PASSWORD=root

# NOTE pédagogique :
# - Ne commite jamais de vraies secrets en clair dans le repo pour un projet public.
# - Garde ce fichier à la racine, copie-le en `.env` et modifie les valeurs locales.
# - En production, utilise un gestionnaire de secrets ou variables d'environnement du serveur.
