<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Réinitialisation de mot de passe - FollowUp</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
            background: #f3f6fb;
            padding: 24px 12px;
            color: #233445;
        }
        
        .email-container {
            max-width: 620px;
            margin: 0 auto;
            background-color: #ffffff;
            border-radius: 16px;
            box-shadow: 0 14px 35px rgba(12, 62, 117, 0.08);
            overflow: hidden;
            border: 1px solid #e6edf5;
        }
        
        .header {
            background: linear-gradient(135deg, #0B77B4 0%, #118fc7 50%, #0b6ba5 100%);
            padding: 36px 28px 28px;
            text-align: center;
            position: relative;
        }

        .header::after {
            content: '';
            position: absolute;
            left: -40px;
            right: -40px;
            bottom: -42px;
            height: 110px;
            background: radial-gradient(circle at 50% 0, rgba(255,255,255,0.18), transparent 60%);
            filter: blur(2px);
        }
        
        .logo-container {
            background: #ffffff;
            width: 112px;
            height: 112px;
            border-radius: 20px;
            margin: 0 auto 16px;
            display: flex;
            align-items: center;
            justify-content: center;
            box-shadow: 0 10px 35px rgba(0, 0, 0, 0.12);
            position: relative;
            z-index: 1;
            padding: 12px;
        }
        
        .logo-img {
            display: block;
            width: 88px;
            height: auto;
        }
        
        .header h1 {
            color: #ffffff;
            font-size: 24px;
            font-weight: 700;
            margin: 0;
            letter-spacing: 0.3px;
            position: relative;
            z-index: 1;
        }
        
        .content {
            padding: 46px 36px 38px;
        }
        
        .greeting {
            font-size: 18px;
            color: #0b2e4d;
            margin-bottom: 14px;
            font-weight: 700;
        }
        
        .message {
            font-size: 15px;
            color: #4a5a6a;
            line-height: 1.65;
            margin-bottom: 24px;
        }
        
        .button-container {
            text-align: center;
            margin: 36px 0 30px;
        }
        
        .reset-button {
            display: inline-block;
            background: linear-gradient(135deg, #0B77B4 0%, #1499D3 100%);
            color: #ffffff;
            text-decoration: none;
            padding: 15px 46px;
            border-radius: 12px;
            font-size: 16px;
            font-weight: 700;
            letter-spacing: 0.2px;
            box-shadow: 0 10px 24px rgba(11, 119, 180, 0.28);
            transition: transform 0.2s ease, box-shadow 0.2s ease;
        }
        
        .reset-button:hover {
            transform: translateY(-1px);
            box-shadow: 0 14px 26px rgba(11, 119, 180, 0.34);
        }
        
        .expiration-notice {
            background-color: #fff7e8;
            border-left: 4px solid #f5a524;
            padding: 14px 16px;
            margin: 24px 0;
            border-radius: 10px;
        }
        
        .expiration-notice p {
            font-size: 14px;
            color: #b06a00;
            margin: 0;
        }
        
        .alternative-link {
            margin-top: 26px;
            padding: 18px;
            background-color: #f4f8fb;
            border-radius: 12px;
            border: 1px solid #e5eef6;
        }
        
        .alternative-link p {
            font-size: 14px;
            color: #4a5a6a;
            margin-bottom: 8px;
        }
        
        .link-text {
            font-size: 13px;
            color: #0B77B4;
            word-break: break-all;
            font-family: 'Courier New', monospace;
            line-height: 1.5;
        }
        
        .footer {
            background-color: #f6f9fc;
            padding: 24px 26px 28px;
            text-align: center;
            border-top: 1px solid #e1e8f0;
        }
        
        .footer p {
            font-size: 13px;
            color: #5a6a7a;
            margin: 4px 0;
        }
        
        .footer-note {
            font-size: 12px;
            color: #97a6b6;
            margin-top: 16px;
            font-style: italic;
        }
        
        .divider {
            height: 1px;
            background: linear-gradient(to right, transparent, #e0e7ef, transparent);
            margin: 26px 0;
        }
    </style>
</head>
<body>
    <div class="email-container">
        <div class="header">
            <div class="logo-container">
                <img src="{{ absolute_url(asset('icons/icon-96x96.png')) }}" alt="Logo FollowUp" class="logo-img">
            </div>
            <h1>FollowUp</h1>
        </div>
        
        <div class="content">
            <p class="greeting">Bonjour {{ user.firstName ?? 'utilisateur' }},</p>
            
            <p class="message">
                Vous avez demandé la réinitialisation de votre mot de passe pour votre compte FollowUp.
                Pour définir un nouveau mot de passe, cliquez sur le bouton ci-dessous :
            </p>
            
            <div class="button-container">
                <a href="{{ resetUrl }}" class="reset-button">Réinitialiser mon mot de passe</a>
            </div>
            
            <div class="expiration-notice">
                <p>
                    <strong>⚠️ Attention :</strong> Ce lien est valable pendant <strong>1 heure</strong>. 
                    Passé ce délai, vous devrez faire une nouvelle demande de réinitialisation.
                </p>
            </div>
            
            <div class="divider"></div>
            
            <p class="message">
                Si vous n'avez pas demandé cette réinitialisation, vous pouvez ignorer cet email en toute sécurité. 
                Votre mot de passe actuel reste inchangé.
            </p>
            
            <div class="alternative-link">
                <p><strong>Le bouton ne fonctionne pas ?</strong></p>
                <p>Copiez et collez ce lien dans votre navigateur :</p>
                <p class="link-text">{{ resetUrl }}</p>
            </div>
        </div>
        
        <div class="footer">
            <p><strong>FollowUp</strong></p>
            <p>Suivez vos candidatures avec sérénité 🌱</p>
            <p class="footer-note">
                Cet email a été envoyé automatiquement, merci de ne pas y répondre.
            </p>
        </div>
    </div>
</body>
</html>
